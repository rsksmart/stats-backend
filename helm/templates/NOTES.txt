Thank you for installing {{ .Chart.Name }}.

Your release is named: {{ .Release.Name }}
Namespace: {{ .Values.namespace }}

To verify the deployment:
  kubectl get pods -n {{ .Values.namespace }} -l "app.kubernetes.io/name={{ include "app.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

To check the service:
  kubectl get svc -n {{ .Values.namespace }} {{ include "app.fullname" . }}

{{- if .Values.targetGroupBinding.enabled }}

TargetGroupBinding is enabled.
Target Group ARN: {{ .Values.targetGroupBinding.targetGroupArn }}
The service is registered with an external ALB managed outside of Kubernetes.
{{- end }}

{{- if and .Values.externalSecrets .Values.externalSecrets.enabled }}

ExternalSecrets is enabled.
Secrets will be synced from AWS Parameter Store.
{{- end }}
